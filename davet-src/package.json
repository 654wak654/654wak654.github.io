{
  "name": "davet",
  "version": "0.0.1",
  "description": "",
  "license": "UNLICENSED",
  "author": "654wak654",
  "scripts": {
    "dev": "parcel index.html -p 50502",
    "prebuild": "rimraf dist \"../davet\"",
    "build": "parcel build index.html --no-source-maps --public-url /davet",
    "postbuild": "node postbuild.mjs"
  },
  "dependencies": {
    "@tailwindcss/postcss": "^4.3.0",
    "parcel": "^2.16.4",
    "rimraf": "^6.1.3",
    "tailwindcss": "^4.3.0"
  },
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.16.4",
    "@parcel/transformer-webmanifest": "^2.16.4"
  }
}
